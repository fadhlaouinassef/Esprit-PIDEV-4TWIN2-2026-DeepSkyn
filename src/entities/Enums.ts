export enum RoleType {
  USER = 'USER',
  PREMIUM_USER = 'PREMIUM_USER',
  ADMIN = 'ADMIN'
}

export enum NiveauBadge {
  BRONZE = 'BRONZE',
  SILVER = 'SILVER',
  GOLD = 'GOLD',
  PLATINUM = 'PLATINUM',
  RUBY_MASTER = 'RUBY MASTER'
}

export enum SkinType {
  OILY = 'OILY',
  DRY = 'DRY',
  SENSITIVE = 'SENSITIVE',
  NORMAL = 'NORMAL',
  COMBINATION = 'COMBINATION'
}

export enum MsgType {
  RECEIVER = 'receiver',
  SENDER = 'sender'
}

export enum EtatComplaint {
  ACCEPT = 'ACCEPT',
  PENDING = 'PENDING',
  REJECT = 'REJECT'
}

export enum EtatFeedback {
  VISIBLE = 'visible',
  INVISIBLE = 'invisible'
}
